---
"@nais/ds-svelte-community": minor
---

Textarea: New component for multi-line text input with auto-resize, character counter, and full parity with React.

TextField: Add `readonly` support with padlock icon, and `showIcon` on error messages to match React.
